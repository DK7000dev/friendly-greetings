{
  "name": "static-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host :: --port 8080",
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html app img dist/ && cp app/index.html dist/app.html",
    "build:dev": "rm -rf dist && mkdir -p dist && cp -r index.html app img dist/ && cp app/index.html dist/app.html",
    "preview": "vite preview --host :: --port 8080"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
