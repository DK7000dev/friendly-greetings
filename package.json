{
  "name": "static-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host :: --port 8080",
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html app img dist/",
    "build:dev": "rm -rf dist && mkdir -p dist && cp -r index.html app img dist/",
    "preview": "vite preview --host :: --port 8080"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
